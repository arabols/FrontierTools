import React from "react";
import LoadingIndicator from "../../app/common/loadingIndicator";

export default function Home() {
    //if (1 == 1) return <LoadingIndicator />
    return (
        <h1>Home</h1>
    )
}